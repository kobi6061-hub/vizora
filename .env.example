# Access password for the private site (checked server-side in /api/login).
SITE_PASSWORD=change-me
# Random secret used to sign session cookies (generate: openssl rand -hex 32).
SESSION_SECRET=change-me-too
